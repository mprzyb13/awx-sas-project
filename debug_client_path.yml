---
- hosts: all
  gather_facts: false

  tasks:
    - name: Check SAS source dir from AWX job context
      delegate_to: 127.0.0.1
      run_once: true
      stat:
        path: /var/lib/awx/projects/client
      register: sas_dir

    - name: Show result
      debug:
        var: sas_dir
